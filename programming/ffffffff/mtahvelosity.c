#include<stdio.h>
 int main()

 {
 	int s,u,t,a;
 	printf(" enter the value Of u   t   a :\n");
 	scanf(  "  %d %d %d",&u,&t,&a);
 	s=(u*t)+.5*(a*t*t);

 	printf(" the result  Is  : %d",s);

 	return 0;
 }